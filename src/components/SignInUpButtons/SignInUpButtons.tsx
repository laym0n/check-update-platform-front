import React from "react";

import {Button} from "@mui/material";

export const SignInButton = () => {
    return (<Button href="/sign-in" variant="contained">Sign in</Button>);
};

export const SignUpButton = () => {
    return (<Button href="/sign-up" variant="contained">Sign up</Button>);
};

